import 'package:flutter/material.dart';

class AuthorizeForm extends StatelessWidget {
  const AuthorizeForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
