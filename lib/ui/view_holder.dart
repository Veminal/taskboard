import 'package:flutter/material.dart';

class ViewHolder extends StatelessWidget {
  const ViewHolder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      color: Colors.black,
    );
  }
}
